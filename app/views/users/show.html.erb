<% provide(:title, "ダッシュボード") %>
<%= content_tag(:div, :class => "dashboard_wrapper") do -%>
  <% if @photos.any? %>
    <% @photos.each do |photo| %>
      <%= render photo %>
    <% end %>
  <% end %>
<% end -%>
