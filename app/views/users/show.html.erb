<% provide(:title, "ダッシュボード") %>

  <%= @user.photos.count %>
  <% @user.photos.each do |photo| %>
    <%= photo.took_date %>
  <% end %>

<% if @user.photos.any? %>
  <%= render @photos %>
<% end %>
