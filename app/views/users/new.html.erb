<% provide(:title, '登録') %>
<h1>ユーザー登録</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "登録", class: "btn" %>
    <% end %>
  </div>
</div>
